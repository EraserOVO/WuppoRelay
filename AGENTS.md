# WuppoRelay 开发规则

## 项目方向

WuppoRelay 是 Discord → QQ 消息转发器。

- 只做 Discord → QQ
- 不做 QQ → Discord
- 不做双向同步

## 修改规则

1. 修改前先说明：
   - 涉及哪些文件
   - 为什么修改
   - 会影响哪些现有功能
2. 每次只处理一个明确问题。
3. 优先最小范围修改，不进行无必要的重构。
4. 不要自行删除现有功能。
5. 不要自行恢复 plugins/discord_history.py 的自动历史补发。
6. 需求不明确或发现需求冲突时，先询问，不要自行决定。

## 文件读取规则

搜索或分析项目时：

- 排除 .venv/
- 排除 backup/
- 排除 __pycache__/
- 排除 data/logs/
- 排除 测试内容/
- 排除 .zcode/
- 不读取 .env.prod

日志只在排查问题时按需读取末尾或搜索相关关键词，不要整份读取。

不要无理由读取整个项目的所有源代码，只读取与当前问题相关的文件。

## 修改前后

修改前先检查当前代码状态。

修改后只检查与本次修改直接相关的功能，不要顺便修改其他部分。

如果修改可能影响现有功能，必须明确说明影响范围。
